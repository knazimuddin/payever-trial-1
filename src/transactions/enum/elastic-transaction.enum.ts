export const enum ElasticTransactionEnum {
  index = 'transactions',
  type = 'transaction',
}

export const enum NewElasticTransactionEnum {
  index = 'folder_transactions',
  type = 'folder_transactions',
}
